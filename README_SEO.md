SEO Quick steps for Krishna Multispeciality Hospital

1) Copy `seo-head.html` contents into the <head> of each HTML page (e.g., `index.html`, `forms-embed.html`). Replace placeholders:
   - Replace `https://www.kmshospitals.com/` with your production URL if different.
   - Update `/assets/og-image.jpg` with a real image (recommended 1200x630)
   - Fill `postalCode`, `latitude`, and `longitude` in the JSON-LD.

2) Create and publish `robots.txt` and `sitemap.xml` at site root (we added those files). Then register the site in Google Search Console and submit the sitemap URL:

   https://search.google.com/search-console

3) Important keywords to use in visible content and headings (don’t keyword-stuff):
   - Sitamarhi dentist
   - dental trauma management
   - general medicine Sitamarhi
   - general surgery Sitamarhi
   - normal delivery Sitamarhi

4) Tips to rank better:
   - Ensure mobile-friendly responsive design (use Google Mobile-Friendly Test).
   - Improve page speed (optimize images, enable compression, use caching).
   - Use unique, descriptive H1/H2 headings on each page including local keywords (e.g., "Dentistry in Sitamarhi — Dental Trauma Management").
   - Add patient-focused content and FAQs for services (dental trauma, RCT, normal delivery, emergency care).
   - Use structured data for Services/MedicalSpecialty as present in `seo-head.html`.

5) Local SEO:
   - Create/claim a Google Business Profile with exact address and phone (+91 9006008932).
   - Add consistent NAP (Name, Address, Phone) markup on all pages.
   - Encourage genuine reviews on Google Maps.

6) Monitoring:
   - Use Google Search Console and Google Analytics to track impressions and queries.

If you want, I can insert the `seo-head.html` content into specific HTML files in the repo — tell me which files (e.g., `index.html`) and I’ll update them.
